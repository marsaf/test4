apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/marsaf/test4:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/marsaf/test4:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: test4
        chartPath: charts/test4
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/marsaf/test4'
          image.tag: '{{.DIGEST_HEX}}'
